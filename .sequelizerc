const path = require("path");

module.exports = {
  "models-path": path.resolve("src", "models"), //kalau exec ke build | create ke src
  "seeders-path": path.resolve("src", "seeders"),
  "migrations-source-path": path.resolve("src", "migrations"),
  "migrations-compiled-path": path.resolve("src", "migrations"),
};

// npx sequelize-cli migration:create --name NewColumn --migrations-path src/migrations
